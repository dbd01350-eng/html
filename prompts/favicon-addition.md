# 파비콘 추가 프롬프트

## 개요
`index.html` 문서에 특정 SVG 파일을 파비콘으로 설정하도록 요청하는 프롬프트입니다.

## 프롬프트
```text
@index.html 문서에 @02/img/noto-v1_cat.svg 를 파비콘으로 넣어
```

## 적용 사항
- `index.html`의 `<head>` 태그 내부에 파비콘 설정 코드 삽입
- SVG 파일 형식에 맞춰 `type="image/svg+xml"` 속성 적용
